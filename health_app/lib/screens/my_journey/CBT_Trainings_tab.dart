import 'package:flutter/material.dart';

class CBTTrainingsTab extends StatelessWidget {
  const CBTTrainingsTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("CBT Trainings will be implemented soon."),
    );
  }
}
